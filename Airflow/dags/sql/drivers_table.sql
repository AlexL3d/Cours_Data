DROP TABLE IF EXISTS drivers_data;

CREATE TABLE IF NOT EXISTS drivers_data (
    "school_year" text,
    "vendor_name" text,
    "type_of_service" text,
    "active_employees" text,
    "job_type" text
);